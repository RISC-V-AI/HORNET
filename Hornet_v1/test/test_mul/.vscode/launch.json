{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C (no debug)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "cwd": "${fileDirname}",         
      "preLaunchTask": "run-c",
      "MIMode": "gdb",
      "miDebuggerPath": "/bin/true"
    }
  ]
}
